console.log("🔥\toverlay active");
const OverlayControl = () => {
	// This controls the overlay. 
	// Overlay applies to video background.
	// ------------------------------------
	const overlay = document.querySelector( "#video-overlay" );
	const siteLinks = document.querySelectorAll( "a[class='site-link']" );
	console.log( siteLinks );
};
OverlayControl();